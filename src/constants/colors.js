const { generate } = require("@ant-design/colors");

const gold = generate("#C09853");
const deepBlue = generate("#00304E");
const white = "#FFFFFF";

module.exports.colors = { gold, deepBlue, white };
